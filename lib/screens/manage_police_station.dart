import 'package:flutter/material.dart';

class ManagePoliceStation extends StatelessWidget {
  const ManagePoliceStation({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}